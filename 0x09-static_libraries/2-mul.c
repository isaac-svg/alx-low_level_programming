#include "main.h"
/**
 * mul - Mulplifies intergers
 * @a: first argument.
 * @b: second argument.
 * Return: integer i.e. a*b.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
